/* generated by Svelte vX.Y.Z */
import {
	SvelteComponent,
	append_experimental,
	claim_element_experimental,
	detach,
	first_child,
	init,
	insert_experimental_hydration,
	make_renderer,
	next_element_sibling,
	noop,
	query_selector_all,
	safe_not_equal
} from "svelte/internal";

const render_1 = make_renderer(`<link rel="canonical" href="/"><meta name="description" content="some description"><meta name="keywords" content="some keywords">`);
const render = make_renderer(`<div>Just a dummy page.</div>`);

function create_fragment(ctx) {
	let link;
	let meta0;
	let meta1;
	let div;

	return {
		c() {
			link = first_child(render_1());
			meta0 = next_element_sibling(link);
			meta1 = next_element_sibling(meta0);
			div = first_child(render());
			this.h();
		},
		l(nodes) {
			this.c();
			const head_nodes = query_selector_all('[data-svelte=\"svelte-1s8aodm\"]', document.head);
			link = claim_element_experimental(link, head_nodes, document.head);
			meta0 = claim_element_experimental(meta0, head_nodes, document.head);
			meta1 = claim_element_experimental(meta1, head_nodes, document.head);
			head_nodes.forEach(detach);
			div = claim_element_experimental(div, nodes);
			this.h();
		},
		h() {
			document.title = "Some Title";
		},
		m(target, anchor) {
			append_experimental(document.head, link);
			append_experimental(document.head, meta0);
			append_experimental(document.head, meta1);
			insert_experimental_hydration(target, div, anchor);
		},
		p: noop,
		i: noop,
		o: noop,
		d(detaching) {
			detach(link);
			detach(meta0);
			detach(meta1);
			if (detaching) detach(div);
		}
	};
}

class Component extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, null, create_fragment, safe_not_equal, {});
	}
}

export default Component;